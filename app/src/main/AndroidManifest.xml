<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:name=".ExpenseTrackerApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ExpenseTracker">

        <!-- Firebase Crashlytics: disabled at the SDK level by default (v3.11.0 / ADR-010).
             Runtime enable happens after explicit user consent via AnalyticsPreferences
             (Task 1.5). This covers the ~100ms init race before consent is wired. -->
        <meta-data
            android:name="firebase_crashlytics_collection_enabled"
            android:value="false" />

        <!-- Firebase Analytics: master gate disabled at the SDK level by default
             (v3.11.0 / ADR-010 / ADR-011 / PRD FR-A8). Runtime enable happens
             after explicit user consent via AnalyticsPreferences.analyticsEventsConsent
             (Task 5.4). Covers the ~100ms init race before the observer runs. -->
        <meta-data
            android:name="firebase_analytics_collection_enabled"
            android:value="false" />

        <!-- FR-A8: suppress the SDK's automatic `screen_view` events so only
             the explicit FR-A6 event list can ever fire. `session_start` and
             `first_open` are not individually disableable but are gated by
             the master collection flag above until consent unlocks them. -->
        <meta-data
            android:name="google_analytics_automatic_screen_reporting_enabled"
            android:value="false" />

        <!-- FR-A7 defense-in-depth: never collect advertising IDs or SSAID
             even if a future contributor accidentally re-enables collection.
             The app is already ad-free; these flags make the SDK honour that
             at its boundary rather than relying on us not calling specific
             APIs. -->
        <meta-data
            android:name="google_analytics_adid_collection_enabled"
            android:value="false" />
        <meta-data
            android:name="google_analytics_ssaid_collection_enabled"
            android:value="false" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:launchMode="singleTop"
            android:theme="@style/Theme.ExpenseTracker">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Disable WorkManager auto-initialization; custom Configuration.Provider is used -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                android:value="androidx.startup"
                tools:node="remove" />
        </provider>

        <!-- Home-screen widget (v3.10.0). Small 2×1 / medium 4×2 variants
             handled by ExpenseWidget's SizeMode.Responsive. -->
        <receiver
            android:name=".widget.ExpenseWidgetReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/expense_widget_info" />
        </receiver>
    </application>

</manifest>